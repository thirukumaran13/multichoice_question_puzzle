{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Manifest",
  "description": "Schema for the questions manifest file",
  "type": "object",
  "required": ["files"],
  "additionalProperties": false,
  "properties": {
    "files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[\\w\\-]+\\.json$",
        "description": "Filename of a question JSON file (must end in .json)"
      },
      "description": "List of question file names available in this directory"
    }
  }
}
